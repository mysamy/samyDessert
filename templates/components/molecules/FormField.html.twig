{% set fieldId = this.computedId %}

<div class="space-y-1">
  <twig:Atoms:Label forId="{{ fieldId }}" label="{{ label }}" />

  {% block control %}{% endblock %}

  {% if error %}
    <p class="text-sm text-red-600" id="{{ this.errorId }}" role="alert">
      {{ error }}
    </p>
  {% endif %}
</div>
