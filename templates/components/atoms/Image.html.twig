{% set src = this.src %}
{% set alt = this.alt %}
{% set loading = this.loading ?: 'lazy' %}

<img
  src="{{ src }}"
  alt="{{ alt is not null ? alt : '' }}"
  loading="{{ loading }}"
  {{ attributes }}
>