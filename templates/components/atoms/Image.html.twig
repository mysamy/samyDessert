{% set src = this.src %}
{% set alt = this.alt %}
{% set class = this.class %}
{% set loading = this.loading %}

<img
  src="{{ src }}"
  alt="{{ alt }}"
  loading="{{ loading }}"
  class="{{ class }}"
/>
