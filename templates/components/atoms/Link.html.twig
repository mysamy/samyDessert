<a
  {% if external %}target="_blank" rel="noopener noreferrer"{% endif %}
  {{ attributes.defaults({ class: 'inline-flex items-center text-accent hover:underline transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded-sm' }) }}
>
  {% block content %}{% endblock %}
</a>

{#
  Props (* = obligatoire) :
  - external  bool — ajoute target="_blank" rel="noopener noreferrer" (false)

  Attributs HTML utiles (via attributes) :
  - href*       — URL de destination
  - aria-label  — si le texte du lien n'est pas explicite

  Utilisation :
  <twig:Atoms:Link href="{{ path('app_home') }}">Accueil</twig:Atoms:Link>
  <twig:Atoms:Link href="https://stripe.com" :external="true">Payer</twig:Atoms:Link>
  <twig:Atoms:Link href="{{ path('app_produits') }}" aria-label="Voir tous les produits">Produits</twig:Atoms:Link>
#}
