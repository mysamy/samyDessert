{% extends 'base.html.twig' %}

{% block title %}Connexion — Samy Dessert{% endblock %}

{% block body %}

<section class="min-h-[80vh] flex items-center justify-center bg-bg py-16 px-4">
  <div class="w-full max-w-md">

    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-text mb-1">Connexion</h1>
      <p class="text-text-light text-sm">Bienvenue ! Connectez-vous à votre compte.</p>
    </div>

    <div class="bg-surface rounded-2xl shadow-sm border border-border p-8">

      <twig:Organisms:LoginForm
        action="{{ path('app_login') }}"
        csrfToken="{{ csrf_token('authenticate') }}"
        emailValue="{{ lastUsername ?? '' }}"
        formError="{{ error ? error.messageKey|trans({}, 'security') : '' }}"
        class="flex flex-col gap-5"
      />

      <p class="mt-6 text-center text-sm text-text-light">
        Pas encore de compte ?
        <twig:Atoms:Link href="{{ path('app_register') }}" class="text-accent font-medium">Créer un compte</twig:Atoms:Link>
      </p>

    </div>
  </div>
</section>

{% endblock %}
