{% extends 'base.html.twig' %}

{% block title %}Mon panier — SamyDessert{% endblock %}

{% block body %}

{# ── Hero ── #}
<section class="section-alt">
  <div class="container-main text-center">
    <h1 class="text-title font-bold text-text mb-2">Mon panier</h1>
    <p class="text-text-light text-body">Vérifiez votre sélection avant de passer commande.</p>
  </div>
</section>

<section class="section">
  <div class="mx-auto max-w-4xl px-4">
    <twig:Organisms:PanierLive :isPage="true" />
  </div>
</section>

{% endblock %}